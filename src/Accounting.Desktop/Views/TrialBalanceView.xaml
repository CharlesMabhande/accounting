<UserControl x:Class="Accounting.Desktop.Views.TrialBalanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource Brush.TextPrimary}" Text="Trial balance" />
    <StackPanel Grid.Row="1" Margin="0,12,0,8" Orientation="Horizontal">
      <TextBlock VerticalAlignment="Center" Margin="0,0,8,0" Text="As of" />
      <DatePicker x:Name="AsOfPicker" Width="160" SelectedDateFormat="Short" />
      <Button Margin="12,0,0,0" Padding="12,6" Content="Load" Click="Load_Click" />
      <Button Margin="8,0,0,0" Padding="12,6" Content="Export CSV…" Click="ExportCsv_Click" />
      <Button Margin="8,0,0,0" Padding="12,6" Content="Export PDF…" Click="ExportPdf_Click" />
      <TextBlock x:Name="StatusText" Margin="16,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource Brush.TextSecondary}" />
    </StackPanel>
    <DataGrid x:Name="Grid"
              Grid.Row="2"
              AutoGenerateColumns="True"
              IsReadOnly="True"
              HeadersVisibility="Column"
              GridLinesVisibility="Horizontal"
              RowHeaderWidth="0"
              AlternatingRowBackground="{DynamicResource Brush.SurfaceMuted}"
              CanUserAddRows="False" />
  </Grid>
</UserControl>
