<UserControl x:Class="Accounting.Desktop.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ct="clr-namespace:Accounting.Desktop.Controls">
  <DockPanel LastChildFill="True" Background="{DynamicResource Brush.SurfacePage}">
    <ct:BrandedFormHeader DockPanel.Dock="Top"
                          Title="About"
                          Subtitle="CharlzTech Accounting"
                          IconGlyph="&#xE946;" />
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  Padding="24,16,24,32">
      <StackPanel MaxWidth="720"
                  HorizontalAlignment="Center"
                  Margin="0,0,0,24">
        <ct:BrandedLogo HorizontalAlignment="Center"
                        LogoHeight="160"
                        CornerRadius="18" />
        <TextBlock Margin="0,24,0,0"
                   HorizontalAlignment="Center"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource Brush.TextPrimary}"
                   Text="CharlzTech Accounting" />
        <TextBlock Margin="0,8,0,0"
                   HorizontalAlignment="Center"
                   FontSize="14"
                   Foreground="{DynamicResource Brush.TextSecondary}"
                   Text="Desktop workspace for finance operations" />
        <TextBlock Margin="0,24,0,0"
                   TextWrapping="Wrap"
                   FontSize="14"
                   LineHeight="24"
                   Foreground="{DynamicResource Brush.TextSecondary}"
                   Text="Modules mirror enterprise accounting concepts; this application is independent software. Branding and theme colours reflect the CharlzTech identity." />
        <TextBlock Margin="0,28,0,0"
                   FontWeight="SemiBold"
                   FontSize="14"
                   Foreground="{DynamicResource Brush.TextPrimary}"
                   Text="Developer" />
        <TextBlock Margin="0,10,0,0"
                   TextWrapping="Wrap"
                   FontSize="14"
                   LineHeight="24"
                   Foreground="{DynamicResource Brush.TextSecondary}"
                   Text="CharlzTech Web Developers (Charles Mabhande)" />
        <TextBlock Margin="0,4,0,0"
                   FontSize="14"
                   Foreground="{DynamicResource Brush.TextSecondary}">
          <Hyperlink NavigateUri="mailto:charliemabhande@gmail.com"
                     RequestNavigate="DeveloperLink_OnRequestNavigate"
                     Foreground="{DynamicResource Brush.Accent}">
            charliemabhande@gmail.com
          </Hyperlink>
        </TextBlock>
        <TextBlock Margin="0,4,0,0"
                   FontSize="14"
                   Foreground="{DynamicResource Brush.TextSecondary}">
          <Hyperlink NavigateUri="tel:+263776318768"
                     RequestNavigate="DeveloperLink_OnRequestNavigate"
                     Foreground="{DynamicResource Brush.Accent}">
            +263 776 318 768
          </Hyperlink>
        </TextBlock>
      </StackPanel>
    </ScrollViewer>
  </DockPanel>
</UserControl>
