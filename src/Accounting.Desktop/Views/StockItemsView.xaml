<UserControl x:Class="Accounting.Desktop.Views.StockItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <StackPanel Grid.Row="0">
      <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource Brush.TextPrimary}" Text="Stock items" />
      <TextBlock Margin="0,4,0,0" Foreground="{DynamicResource Brush.TextSecondary}" Text="Inventory items with extended dimensions (length, width, weight) for products such as belts."
                 TextWrapping="Wrap" />
    </StackPanel>
    <StackPanel Grid.Row="1" Margin="0,12,0,8" Orientation="Horizontal">
      <Button Padding="12,6" Content="Refresh" Click="Refresh_Click" />
      <Button Margin="8,0,0,0" Padding="12,6" Content="Add" Click="Add_Click" />
      <Button Margin="8,0,0,0" Padding="12,6" Content="Edit" Click="Edit_Click" />
      <Button Margin="8,0,0,0" Padding="12,6" Content="Delete" Click="Delete_Click" />
      <Button Margin="8,0,0,0" Padding="12,6" Content="Export CSV…" Click="ExportCsv_Click" />
      <Button Margin="8,0,0,0" Padding="12,6" Content="Export PDF…" Click="ExportPdf_Click" />
      <TextBlock x:Name="StatusText" Margin="16,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource Brush.TextSecondary}" />
    </StackPanel>
    <DataGrid x:Name="Grid"
              Grid.Row="2"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              HeadersVisibility="Column"
              GridLinesVisibility="Horizontal"
              RowHeaderWidth="0"
              AlternatingRowBackground="{DynamicResource Brush.SurfaceMuted}"
              CanUserAddRows="False"
              SelectionMode="Single">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="*" />
        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*" />
        <DataGridTextColumn Header="Alt" Binding="{Binding AlternateCode}" Width="80" />
        <DataGridTextColumn Header="UoM" Binding="{Binding UnitOfMeasure}" Width="60" />
        <DataGridTextColumn Header="Active" Binding="{Binding IsActive}" Width="60" />
        <DataGridTextColumn Header="Service" Binding="{Binding IsServiceItem}" Width="60" />
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>
