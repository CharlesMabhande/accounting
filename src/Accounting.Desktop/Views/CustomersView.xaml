<UserControl x:Class="Accounting.Desktop.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <StackPanel Grid.Row="0">
      <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource Brush.TextPrimary}" Text="Customers" />
      <TextBlock Margin="0,4,0,0" Foreground="{DynamicResource Brush.TextSecondary}" TextWrapping="Wrap"
                 Text="Add, edit, and remove customers. Double-click a row to edit. Print statements for posted invoices and receipts." />
    </StackPanel>
    <StackPanel Grid.Row="1" Margin="0,12,0,8" Orientation="Horizontal">
      <Button Padding="12,6" Content="Refresh" Click="Refresh_Click" />
      <Button Margin="8,0,0,0" Padding="12,6" Content="Add" Click="Add_Click" />
      <Button Margin="8,0,0,0" Padding="12,6" Content="Edit" Click="Edit_Click" />
      <Button Margin="8,0,0,0" Padding="12,6" Content="Delete" Click="Delete_Click" />
      <Button Margin="8,0,0,0" Padding="12,6" Content="Export CSV…" Click="ExportCsv_Click" />
      <Button Margin="8,0,0,0" Padding="12,6" Content="Export PDF…" Click="ExportPdf_Click" />
      <Button Margin="8,0,0,0"
              Padding="12,6"
              Content="Print statement…"
              Style="{DynamicResource AccentPrimaryButton}"
              Click="PrintStatement_Click" />
      <TextBlock x:Name="StatusText" Margin="16,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource Brush.TextSecondary}" />
    </StackPanel>
    <DataGrid x:Name="Grid"
              Grid.Row="2"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              MouseDoubleClick="Grid_OnMouseDoubleClick"
              HeadersVisibility="Column"
              GridLinesVisibility="Horizontal"
              RowHeaderWidth="0"
              AlternatingRowBackground="{DynamicResource Brush.SurfaceMuted}"
              CanUserAddRows="False"
              SelectionMode="Single">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="*" />
        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" />
        <DataGridTextColumn Header="AR ledger" Binding="{Binding AccountsReceivableAccountCode}" Width="*" />
        <DataGridTextColumn Header="Currency" Binding="{Binding CurrencyCode}" Width="80" />
        <DataGridTextColumn Header="Active" Binding="{Binding IsActive}" Width="70" />
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>
